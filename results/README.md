# Experiment results

`metrics.csv` is the version-controlled aggregate experiment ledger used for
reproducible comparisons. Per-run checkpoints and detailed artefacts are stored
under `results/runs/` and remain ignored because they can be regenerated.
